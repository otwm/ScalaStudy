Console print "test"

def max(keyBoard: String, target: String, inputLength: Int): Int = {
  //ABA BA A
  //ABA AB A
  println(target.substring(target.length))
  for(index <- 0 to target.length){
    println(target.substring(target.length))
  }
  0
}

max("A","A",1) //1

println("AA".substring("AA".length))